import React, { FC } from 'react';
import PaymentAccount from '../../sections/PaymentAccount';

const Payments: FC = () => {
  return (
    <div>
      <PaymentAccount />
    </div>
  );
};

export default Payments;
